project: default
source:
  repoURL: 'https://prometheus-community.github.io/helm-charts'
  targetRevision: 14.0.1
  chart: kube-prometheus-stack
destination:
  namespace: prometheus
  name: lk-test.k8s.local
syncPolicy:
  syncOptions:
    - CreateNamespace=true
